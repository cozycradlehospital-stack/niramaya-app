# Copy this file to .env.local and fill in your real values (Supabase dashboard → Settings → API).
# Never commit the real .env.local — it's already in .gitignore.
VITE_SUPABASE_URL=https://gzxzlrijpqezkmxyvqci.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here
